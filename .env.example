# All optional — the app runs fully without any of these (curated catalog mode).

# Neon Postgres connection string (auto-set by the Vercel Neon integration)
DATABASE_URL=

# Protects POST /api/seed when set; pass as ?token=...
SEED_TOKEN=
